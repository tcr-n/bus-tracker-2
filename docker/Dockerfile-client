##########################################
# STAGE 1 - Build project artifacts      #
##########################################

FROM node:26.8.2-alpine AS builder
WORKDIR /bus-tracker
RUN npm install -g --force corepack@latest
RUN corepack enable

# Fetch dependencies
# COPY patches/ ./patches/
# The root package.json must be present before the first pnpm invocation so that
# corepack pins the version declared in `packageManager` instead of the latest one.
COPY package.json pnpm-lock.yaml pnpm-workspace.yaml ./
RUN pnpm fetch

# Install dependencies
COPY scripts/ ./scripts/
COPY libraries/contracts/ ./libraries/contracts/
COPY apps/client/ ./apps/client/
RUN pnpm install -r

# Inject build information
ARG BUILD_HASH
ENV VITE_BUILD_HASH=${BUILD_HASH}

ARG BUILD_TIMESTAMP
ENV VITE_BUILD_TIMESTAMP=${BUILD_TIMESTAMP}

# Build client, then upload and remove source maps from the resulting image.
RUN --mount=type=secret,id=posthog_cli_api_key,required=false \
	--mount=type=secret,id=posthog_cli_project_id,required=false \
	--mount=type=secret,id=posthog_cli_host,required=false \
	pnpm -r build && \
	sh scripts/upload-posthog-sourcemaps.sh apps/client apps/client/dist bus-tracker-client "${VITE_BUILD_HASH:-dev}"

##########################################
# STAGE 2 - Run distribution app         #
##########################################

FROM nginxinc/nginx-unprivileged:1.31.5-alpine AS web

USER root
RUN apk add --no-cache curl tini
USER 101

# Copy nginx configuration
COPY docker/nginx.conf /etc/nginx/nginx.conf

# Copy build artifacts
WORKDIR /usr/share/nginx/html
COPY --from=builder /bus-tracker/apps/client/dist/ ./

ENTRYPOINT ["/sbin/tini", "--"]
CMD ["nginx", "-g", "daemon off;"]
